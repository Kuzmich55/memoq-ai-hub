## What's New

- Fixed the Assets page so uploaded custom TM files now have a visible import entry from the main asset manager.
- The left navigation and asset manager copy now use the broader Assets label because this page manages glossary, termbase, and custom TM files.
- The Assets page add menu now includes Custom TM, the sidebar includes a Custom TM category, and the allowed extensions list shows `.tmx` and table memory formats.

## Notes

- This is a UI entry-point follow-up to v1.0.20.
- The custom TM matching engine, `AI Hub TM score`, memoQ-style buckets, and TMX parsing behavior are unchanged from v1.0.20.
